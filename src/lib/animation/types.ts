export type AnimUtils = Window['animUtils'];

export interface AnimConfig {
  duration: Window['animUtils']['ANIM']['duration'];
  ease: Window['animUtils']['ANIM']['ease'];
  blur: Window['animUtils']['ANIM']['blur'];
  y: Window['animUtils']['ANIM']['y'];
  stagger: Window['animUtils']['ANIM']['stagger'];
  scroll: Window['animUtils']['ANIM']['scroll'];
}
